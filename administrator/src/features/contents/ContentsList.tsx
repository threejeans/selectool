import React from "react";
type ContentsList = {
  type: "self" | "with" | "guide";
};
const ContentsList = ({ type }: ContentsList) => {
  return <div>ContentsList</div>;
};

export default ContentsList;
